version: "3.6"

services:
  db:
    image: postgres:12.3-alpine
    restart: always
    environment:
      POSTGRES_DB: jivala
      POSTGRES_USER: jivala
      POSTGRES_PASSWORD: jivala
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  db-data: {}



